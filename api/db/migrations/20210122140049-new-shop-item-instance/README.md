# Migration `20210122140049-new-shop-item-instance`

This migration has been generated by las1m0n at 1/22/2021, 4:00:49 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "Shop" (
    "id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    "name" TEXT NOT NULL,
    "imageUrl" TEXT NOT NULL
)

CREATE TABLE "ShopItem" (
    "id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    "url" TEXT NOT NULL DEFAULT 'google.com',
    "itemId" INTEGER,
    "shopId" INTEGER NOT NULL,

    FOREIGN KEY ("shopId") REFERENCES "Shop"("id") ON DELETE CASCADE ON UPDATE CASCADE,
    FOREIGN KEY ("itemId") REFERENCES "Item"("id") ON DELETE SET NULL ON UPDATE CASCADE
)

PRAGMA foreign_keys=OFF;
CREATE TABLE "new_Item" (
    "id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    "name" TEXT NOT NULL,
    "about" TEXT NOT NULL DEFAULT 'Product'
);
INSERT INTO "new_Item" ("id", "name") SELECT "id", "name" FROM "Item";
DROP TABLE "Item";
ALTER TABLE "new_Item" RENAME TO "Item";
PRAGMA foreign_key_check;
PRAGMA foreign_keys=ON
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20210121114555-create..20210122140049-new-shop-item-instance
--- datamodel.dml
+++ datamodel.dml
@@ -1,7 +1,7 @@
 datasource DB  {
   provider = ["sqlite", "postgresql"]
-  url = "***"
+  url = "***"
 }
 generator client {
   provider      = "prisma-client-js"
@@ -15,12 +15,23 @@
   item Item? @relation(fields: [itemId], references: [id])
   itemId  Int?
 }
+model Shop {
+  id        Int      @id @default(autoincrement())
+  name String
+  imageUrl String
+}
+
+model ShopItem {
+  id  Int @id @default(autoincrement())
+  shop Shop
+  url String @default("google.com")
+}
+
 model Item {
   id    Int     @id @default(autoincrement())
-  shop String
   name String
-  url String @default("1")
-  currentPrice Float
+  about String @default("Product")
+  priceInfo ShopItem[]
   priceMap PriceMap[]
 }
```


